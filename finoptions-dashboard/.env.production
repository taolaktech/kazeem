# Used by `npm run build` (Vite's production mode). Local `npm run dev` reads
# `.env` instead, so development keeps pointing at the local API.
# Browsers block plain-http requests from an https page, so this must stay https
# whenever the dashboard itself is served over https.
VITE_API_BASE_URL=https://api.lightworktrading.site
