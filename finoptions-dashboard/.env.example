# Base URL of the NestJS trading intelligence API, read by `npm run dev`.
# Production builds read `.env.production` instead; override either with a
# VITE_API_BASE_URL environment variable in the build/deploy pipeline.
VITE_API_BASE_URL=http://localhost:3000
